// Placeholder — substituir pelo output real de:
//   npx supabase gen types typescript --project-id [SEU_ID] > lib/supabase/types.ts
// Gerar assim que o projeto Supabase dev estiver criado e as migrations aplicadas.

export type Database = any;
